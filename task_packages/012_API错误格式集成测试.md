# 任务 012：API 错误格式集成测试

> **身份**：你只负责这个任务包。不要查看或修改本任务包未指定的任何文件。
> 你是一个没有大局观的 AI，只能编写测试文件。涉及修改生产代码或全局架构决策你会产生幻觉，必须拒绝执行并向上级报告。

---

## 边界规则（不可违反）

1. 只在 `tests/` 目录下创建新文件 `tests/test_error_format.py`。
2. 不改 `app/`、`interface/`、`config.py` 下的任何文件。
3. 不修改现有的测试文件。
4. 如果有任何不确定的地方，问清楚再动手，不要猜测。

---

## 前置准备

读取以下文件：
- `app/api/server.py`（重点关注 `_error_response` 函数和 `ERR_*` 常量，约第 70-83 行）
- `API_DOCS.md`（了解现有 API 文档中的错误格式说明）

---

## 需求

为 API 统一错误格式编写测试，验证 `_error_response` 函数和实际 HTTP 端点的错误响应格式。

### 测试内容

**单元测试：_error_response 函数**

```python
def test_error_response_format():
    """_error_response 应该返回正确的格式"""
    response = _error_response('TEST_ERROR', '测试错误消息', status=400)
    data = json.loads(response.body)

    assert data['ok'] is False
    assert data['error']['code'] == 'TEST_ERROR'
    assert data['error']['msg'] == '测试错误消息'
    assert response.status == 400
```

**集成测试：认证失败的错误格式**

```python
@pytest.mark.asyncio
async def test_auth_failed_format(aiohttp_client, app):
    """认证失败应该返回统一错误格式"""
    client = await aiohttp_client(app)

    resp = await client.post('/api/auth', json={'code': 'wrong'})
    assert resp.status == 400

    data = await resp.json()
    assert data['ok'] is False
    assert 'error' in data
    assert 'code' in data['error']
    assert 'msg' in data['error']
```

**集成测试：文件上传失败的错误格式**

```python
@pytest.mark.asyncio
async def test_upload_failed_format(aiohttp_client, app):
    """文件上传失败应该返回统一错误格式"""
    client = await aiohttp_client(app)

    # 模拟一个不存在的文件类型
    resp = await client.post('/api/upload', data={'file': io.BytesIO(b'test')})
    # 验证格式...（根据实际实现调整）
```

**集成测试：成功响应不应有 error 字段**

```python
@pytest.mark.asyncio
async def test_success_response_no_error(aiohttp_client, app):
    """成功响应不应该包含 error 字段"""
    client = await aiohttp_client(app)

    resp = await client.get('/health')
    data = await resp.json()

    assert 'error' not in data
```

---

## 完整测试文件结构

```python
"""API 错误格式统一测试"""
import pytest
import json
import sys
import os

sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..'))

from app.api.server import _error_response, ERR_AUTH_FAILED, ERR_FILE_TYPE, ERR_INTERNAL


class TestErrorResponseUnit:
    """_error_response 函数单元测试"""

    def test_error_response_basic_format(self):
        """基本错误响应格式"""
        response = _error_response('TEST_ERROR', 'test msg', status=400)
        data = json.loads(response.body)
        assert data == {
            'ok': False,
            'error': {'code': 'TEST_ERROR', 'msg': 'test msg'}
        }
        assert response.status == 400

    def test_error_response_with_status_500(self):
        """500 错误响应"""
        response = _error_response(ERR_INTERNAL, '内部错误', status=500)
        data = json.loads(response.body)
        assert data['ok'] is False
        assert data['error']['code'] == ERR_INTERNAL
        assert data['error']['msg'] == '内部错误'
        assert response.status == 500

    def test_error_response_auth_failed(self):
        """认证失败错误码"""
        response = _error_response(ERR_AUTH_FAILED, '暗号错误', status=400)
        data = json.loads(response.body)
        assert data['error']['code'] == ERR_AUTH_FAILED

    def test_error_response_file_type(self):
        """文件类型错误码"""
        response = _error_response(ERR_FILE_TYPE, '文件类型不允许: .exe', status=400)
        data = json.loads(response.body)
        assert data['error']['code'] == ERR_FILE_TYPE
        assert '.exe' in data['error']['msg']

    def test_error_response_no_body(self):
        """错误响应不应该包含多余字段"""
        response = _error_response(ERR_INTERNAL, 'error', status=400)
        data = json.loads(response.body)
        # 只允许 'ok' 和 'error' 两个顶层字段
        assert set(data.keys()) == {'ok', 'error'}
        assert set(data['error'].keys()) == {'code', 'msg'}
```

---

## 验收标准

1. `pytest tests/test_error_format.py -v` 全部测试通过
2. 测试覆盖：基本格式、自定义状态码、各种错误码、字段完整性

---

## 如何验证

```bash
# 运行测试
cd E:/project/tools/zero
python -m pytest tests/test_error_format.py -v

# 预期输出
# test_error_format.py::TestErrorResponseUnit::test_error_response_basic_format PASSED
# test_error_format.py::TestErrorResponseUnit::test_error_response_with_status_500 PASSED
# ...
```

---

## 遇到问题时的决策树

1. `_error_response` 函数签名不同 → 读取实际代码，按实际签名调用
2. 集成测试部分（HTTP 请求）无法运行 → 只保留单元测试部分，集成测试标记为 `@pytest.mark.skip`
3. `ERR_*` 常量不存在 → 在 server.py 中搜索实际的错误码常量名
4. 导入报错 → 检查 `sys.path` 是否正确添加