# 任务 G：前端无障碍与键盘支持

> **身份**：你只负责这个任务，不要查看或修改本任务包未指定的任何文件。
> 你是一个没有大局观的 AI，只能在前端 HTML 和 JS 文件中添加 ARIA 属性和键盘事件处理。涉及后端逻辑或重构现有功能你会产生幻觉，必须拒绝执行并向上级报告。

---

## 边界规则（不可违反）

1. 只改 `interface/webapp_static/index.html` 和 `interface/webapp_static/app.js` 这两个文件。
2. 在 `index.html` 中只**添加属性**（`aria-*`、`role`），不修改标签结构，不删现有属性，不改变布局。
3. 在 `app.js` 中只**追加键盘事件处理逻辑**，不修改现有的事件绑定。
4. 不改 `style.css`、不改后端文件。
5. 不改 SSE 流逻辑、消息渲染逻辑、主题切换逻辑。
6. 如果有任何不确定的地方，问清楚再动手，不要猜测。

---

## 前置准备

读取以下文件：
- `interface/webapp_static/index.html`（现有全部）
- `interface/webapp_static/app.js`（现有全部，重点关注 `handleKey`、`closeLightbox`、`openLightbox` 函数）

---

## 需求

### HTML 部分（`index.html`）

为现有交互控件添加 ARIA 属性，**只添加属性，不改标签和内容**：

| 位置 | 当前元素 | 添加的属性 |
|------|----------|-----------|
| 登录输入框 | `<input id="code">` | `aria-label="暗号输入"` |
| 解锁按钮 | `<button class="login-btn">` | `aria-label="解锁"` |
| 新对话按钮 | `<button class="new-chat-btn">` | `aria-label="新建对话"` |
| 菜单按钮 | `<button class="menu-btn">` | `aria-label="打开菜单"` |
| 发送按钮 | `<button class="send-btn">` | `aria-label="发送消息"` |
| 附件按钮 | `<button class="attach-btn">` | `aria-label="添加文件"` |
| 对话列表 | `<div class="conversations">` | `role="list"` |
| 每个对话项 | `<div class="conv-item">` | `role="listitem"` |
| 输入框 | `<textarea id="inp">` | `aria-label="消息输入"` |
| 消息区域 | `<div class="messages">` | `role="log"` `aria-live="polite"` `aria-label="聊天消息"` |
| 图片灯箱 | `<div class="lightbox-overlay">` | `role="dialog"` `aria-modal="true"` `aria-label="图片预览"` |
| 灯箱关闭按钮 | `<button class="lightbox-close">` | `aria-label="关闭预览"` |
| 灯箱下载按钮 | `<button class="lightbox-download">` | `aria-label="保存图片"` |

### JS 部分（`app.js` 文件末尾追加键盘事件逻辑）

**追加函数**（不要修改现有函数）：

1. **全局键盘导航函数**：

```javascript
// ===== 全局键盘导航 =====
document.addEventListener('keydown', function(e) {
  // Esc 关闭弹窗
  if (e.key === 'Escape') {
    // 关闭灯箱
    const lb = document.getElementById('lightbox');
    if (lb && lb.classList.contains('show')) {
      closeLightbox();
      e.preventDefault();
      return;
    }

    // 关闭确认弹窗（已由 confirmDialog 处理，这里不需要重复处理）
  }

  // Ctrl+Enter 发送消息（备选快捷键）
  if ((e.ctrlKey || e.metaKey) && e.key === 'Enter') {
    const inp = document.getElementById('inp');
    if (inp === document.activeElement && inp.value.trim()) {
      e.preventDefault();
      send();
    }
  }
});
```

2. **焦点管理函数**：

```javascript
// ===== 焦点管理 =====
function trapFocus(container, event) {
  // 在弹窗打开时循环 Tab 焦点
  const focusable = container.querySelectorAll(
    'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])'
  );
  if (focusable.length === 0) return;

  const first = focusable[0];
  const last = focusable[focusable.length - 1];

  if (event.key === 'Tab') {
    if (event.shiftKey) {
      if (document.activeElement === first) {
        event.preventDefault();
        last.focus();
      }
    } else {
      if (document.activeElement === last) {
        event.preventDefault();
        first.focus();
      }
    }
  }
}
```

3. **图片灯箱焦点管理增强**：

```javascript
// 在灯箱中启用焦点循环
document.getElementById('lightbox').addEventListener('keydown', function(e) {
  if (e.key === 'Tab') {
    trapFocus(this, e);
  }
});
```

4. **消息列表键盘支持**：

```javascript
// 消息列表中的消息可以用上下箭头选择
// 注意：这是一个低优先级的增强，如果实现复杂则跳过
```

### 约束

- 所有 ARIA 属性只添加在 `index.html` 中已存在的元素上。
- 键盘事件处理是**新增的监听器**，不修改现有的 `onkeydown`、`onclick` 等属性或监听器。
- 不删除或修改现有的 `onkeydown="if(event.key==='Escape')closeLightbox()"` 属性。

---

## 验收标准

1. 使用屏幕阅读器（NVDA / Windows 讲述人 / VoiceOver）时：
   - 登录输入框朗读为"暗号输入，编辑文本"
   - 发送按钮朗读为"发送消息，按钮"
   - 聊天消息区域朗读为"聊天消息，日志区域"
   - 图片灯箱朗读为"图片预览，对话框"

2. 键盘操作：
   - Tab 键可以循环到所有按钮和输入框
   - Enter 键发送消息
   - Esc 键关闭灯箱和弹窗
   - Ctrl+Enter 也发送消息

3. 图片灯箱中 Tab 键在控件之间循环（关闭按钮 ↔ 下载按钮）

---

## 如何验证

```bash
# 启动服务
python zero_server.py

# 使用键盘验证：
# 1. Tab 键测试：从登录页面开始，按 Tab 键看焦点是否依次经过：输入框 → 解锁按钮
# 2. Enter 键发送：焦点在输入框时按 Enter，消息发送
# 3. Ctrl+Enter：焦点在输入框时按 Ctrl+Enter，消息发送
# 4. Esc 键：打开图片灯箱后按 Esc，灯箱关闭

# 使用屏幕阅读器验证（以 Windows 讲述人为例）：
# 1. 按 Win+Ctrl+Enter 启动讲述人
# 2. 用 Tab 键导航到各个控件，听朗读内容是否正确
```

---

## 遇到问题时的决策树

1. 不确定某个元素应该加什么 aria 属性 → 停止，问清楚
2. `trapFocus` 实现复杂，不确定是否能正常工作 → 简化实现，只处理最基本的 Tab 循环
3. 需要改 CSS 才能实现无障碍 → 停止，报告（无障碍不应该影响视觉样式）
4. 发现现有代码有 bug → 记录但不修改，报告
5. 不确定 ARIA 属性值的中文翻译 → 使用标准的英文 label，如 `aria-label="Close"`